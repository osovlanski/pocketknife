# Pocketknife CI/CD Pipeline
# Triggered on push to main branch
# Runs tests, linting, and deploys to cloud services

name: Deploy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '20'

jobs:
  # ============================================
  # Lint and Type Check
  # ============================================
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Generate Prisma client
        working-directory: backend
        run: npx prisma generate

      - name: Lint backend
        working-directory: backend
        run: npm run lint --if-present

      - name: Lint frontend
        working-directory: frontend
        run: npm run lint --if-present

      - name: Type check backend
        working-directory: backend
        run: npx tsc --noEmit

      - name: Type check frontend
        working-directory: frontend
        run: npx tsc --noEmit

  # ============================================
  # Test Suite
  # ============================================
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: lint
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install backend dependencies
        working-directory: backend
        run: npm ci

      - name: Generate Prisma client
        working-directory: backend
        run: npx prisma generate

      - name: Run backend tests
        working-directory: backend
        run: npm run test:coverage
        env:
          NODE_ENV: test

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Run frontend tests
        working-directory: frontend
        run: npm run test:coverage

  # ============================================
  # Build Test
  # ============================================
  build:
    name: Build Test
    runs-on: ubuntu-latest
    needs: [lint, test]
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: |
            backend/package-lock.json
            frontend/package-lock.json

      - name: Install and build backend
        working-directory: backend
        run: |
          npm ci
          npx prisma generate
          npm run build

      - name: Install and build frontend
        working-directory: frontend
        run: |
          npm ci
          npm run build

  # ============================================
  # Deploy Backend to Railway
  # ============================================
  # NOTE: Railway deployment is handled automatically via Railway's GitHub integration.
  # Railway watches the 'main' branch and auto-deploys when tests pass.
  # 
  # To set up Railway auto-deploy:
  # 1. Go to Railway Dashboard → Your Project
  # 2. Click on the backend service
  # 3. Go to Settings → Source
  # 4. Connect to GitHub repo: osovlanski/pocketknife
  # 5. Set Root Directory: /backend
  # 6. Set Branch: main
  # 
  # This job just confirms the deploy is triggered.
  deploy-backend:
    name: Deploy Backend (Railway Auto-Deploy)
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Railway deployment notification
        run: |
          echo "✅ Build passed! Railway will auto-deploy from the main branch."
          echo "📦 Backend deployment is handled by Railway's GitHub integration."
          echo "🔗 Check status at: https://railway.app/dashboard"

  # ============================================
  # Deploy Frontend to Vercel
  # ============================================
  # NOTE: Vercel deployment is handled automatically via Vercel's GitHub integration.
  # Vercel watches the 'main' branch and auto-deploys when changes are pushed.
  #
  # To set up Vercel auto-deploy:
  # 1. Go to Vercel Dashboard → pocketknife project
  # 2. Go to Settings → Git
  # 3. Connect to GitHub repo: osovlanski/pocketknife
  # 4. Set Root Directory: frontend
  # 5. Set Production Branch: main
  #
  # This job just confirms the deploy is triggered.
  deploy-frontend:
    name: Deploy Frontend (Vercel Auto-Deploy)
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    
    steps:
      - name: Vercel deployment notification
        run: |
          echo "✅ Build passed! Vercel will auto-deploy from the main branch."
          echo "📦 Frontend deployment is handled by Vercel's GitHub integration."
          echo "🔗 Check status at: https://vercel.com/dashboard"

  # ============================================
  # Database Migrations
  # ============================================
  # NOTE: Migrations run automatically in the Docker container on startup.
  # See backend/Dockerfile CMD which runs: npx prisma migrate deploy
  # 
  # This eliminates the need for DATABASE_URL in GitHub secrets.
  # If you prefer to run migrations in CI instead:
  # 1. Add DATABASE_URL to GitHub Secrets
  # 2. Uncomment the migrate job below
  #
  # migrate:
  #   name: Run Migrations
  #   runs-on: ubuntu-latest
  #   needs: deploy-backend
  #   if: github.event_name == 'push' && github.ref == 'refs/heads/main'
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-node@v4
  #       with:
  #         node-version: ${{ env.NODE_VERSION }}
  #     - working-directory: backend
  #       run: npm ci
  #     - working-directory: backend
  #       run: npx prisma migrate deploy
  #       env:
  #         DATABASE_URL: ${{ secrets.DATABASE_URL }}








