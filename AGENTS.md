# Agent Guidance

Read `docs/PROJECT_MEMORY.md`, existing root architecture/setup reviews, and query `graphify-out/graph.json` before broad searches. Pocketknife is an integration-heavy personal assistant; preserve auth, encryption, user scoping, scheduler idempotency, and external-service fallbacks. Run `npm test` and relevant builds. Never add real credentials or personal data.
